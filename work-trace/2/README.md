# Issue 2 Work Trace

Issue: https://github.com/wonop-io/safenpx/issues/2

## Request

Parse supported exact-version package specs for M1.

## Scope

- Support `name@version` and `@scope/name@version`.
- Preserve forwarded args after `--`.
- Reject unsupported forms before resolver/network work.
- Add parser tests for scoped packages, forwarded args, unsupported specs, and
  malformed specs.

## First Commit Rule

This trace scaffold is committed before implementation begins.

