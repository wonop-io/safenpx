# Work Trace: Issue 54

Issue: #54 M3: Wire inspect mode pipeline without execution

Purpose: add the first M3 inspect-mode pipeline that resolves, downloads,
verifies, extracts evidence, renders output, and stops without package
execution.
