# Context

- `crates/safe-npx/src/lib.rs` currently owns CLI parsing and report rendering.
- `RootArtifactResolver` from issue #3 provides verified root artifact evidence.
- M1 reports should remain intentionally smaller than the later M3 evidence
  model.
