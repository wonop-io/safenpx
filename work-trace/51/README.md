# Issue 51 Work Trace

Issue: https://github.com/wonop-io/safenpx/issues/51

## Goal

Wire M2 `execution_refused` output so unproven execution closures fail closed
with stable human and JSON reasons instead of generic inspection errors.

