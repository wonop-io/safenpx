# M2 Execution Mechanism Decision

This decision record must compare:

- `direct_extract`,
- `pinned_delegation`,
- `inspect_only_alpha`.

It must choose exactly one first alpha path, record rejected alternatives, and
map the decision to completed M2 fixture evidence.

