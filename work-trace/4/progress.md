# Progress

- Moved issue #4 to `status:in-progress`.
- Created trace scaffold before the decision record.
- Recorded the M2 execution mechanism decision:
  `direct_extract` is the first alpha execution path, while
  `pinned_delegation` is rejected and `inspect_only_alpha` remains the fallback
  for unsupported closures.
