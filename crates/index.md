# Crates Index

First-party Rust crates live here.

## Crates

- `safe-npx/index.md`: CLI scaffold for the package execution gate.

## Build

- Build all crates with `bazel build //crates/...`.
- Test all crates with `bazel test //crates/...`.
- Use `cargo fmt --check` for Rust formatting.
