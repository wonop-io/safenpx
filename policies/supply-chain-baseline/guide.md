# Supply Chain Baseline Policy

The current baseline requires Cargo and Bazel lockfiles plus CI coverage gates.

Future work should add RustSec/cargo-audit or cargo-deny once advisory baselines are curated.
