# File Size Policy

Keep source and policy files small enough to review. The default limit is 500 lines.

Fix by splitting large files by responsibility, or justify a raised limit with `FILE_SIZE_MAX_LINES` while refactoring.
