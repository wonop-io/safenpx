# TypeScript Exceptions Policy

Do not add TypeScript unless the repo intentionally grows a TypeScript surface.

Fix by using Rust/shell or by documenting and approving an exception before adding the files.
