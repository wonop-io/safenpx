# No JavaScript Policy

`safe-npx` is a Rust-first security tool. Avoid adding JavaScript source unless a future design explicitly requires it.

Fix by implementing tooling in Rust or shell, or by proposing a policy exception first.
