# Replicated Code Policy

Avoid copy/paste implementation drift. This repository keeps the policy slot for parity and will grow a stricter duplicate-block detector as the codebase grows.

Fix by extracting shared logic when duplicate source blocks appear.
