# Clean Repo Before Tests Policy

This optional gate mirrors Wonop's clean-repo policy. It is opt-in for `safe-npx` because agents often run policy checks while editing.

Set `REQUIRE_CLEAN_REPO=1` before final verification when you want this enforced.
