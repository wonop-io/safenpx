# Local Secret Paths Policy

Secret-shaped filenames are blocked even when their contents look harmless. Keep credentials in your local environment, a secret manager, or GitHub Actions secrets.

Fix by removing the file from git and replacing it with documented placeholder configuration when needed.
